## ファイル移動設定 詳細マニュアル

この設定は、CAD Support Tool の「ファイル移動機能」の動作を制御します。
YAML ファイル内の `move_of_file_1` ～ `move_of_file_3` を変更することで、移動先や除外ファイルなどを自由に設定できます。

---

## 基本構造

```yaml
## ファイル移動設定
move_of_file_1:
  label: "CADフォルダへ"

  out_of_folder: "D:\\CAD_start"
  into_folder: "D:\\CAD_end"

  bg_color: "#00a3af"
  str_color: "white"

  ignore:
    - "tool"
    - "readme.txt"
    - "template.sx"
```

---

## 各パラメータの詳細

---

## move_of_file_1

```yaml
move_of_file_1:
```

### 説明

ファイル移動設定の識別名です。

現在のアプリでは：

* `move_of_file_1`
* `move_of_file_2`
* `move_of_file_3`

の3種類を切り替えて使用できます。

---

## label

```yaml
label: "CADフォルダへ"
```

### 説明

ステータスバーに表示される名称です。

### 表示例

```text
ファイル移動 : CADフォルダへ
```

### 使用例

```yaml
label: "レーザー加工機へ"
```

```yaml
label: "共有フォルダへ"
```

---

## out_of_folder

```yaml
out_of_folder: "D:\\CAD_start"
```

### 説明

ファイル移動元フォルダです。

このフォルダ内のファイルを監視し、移動対象を検索します。

### 注意点

* フォルダが存在しない場合はエラーになります
* ダブルクリック時にこのフォルダが開きます

### 使用例

```yaml
out_of_folder: "C:\\CAD"
```

```yaml
out_of_folder: "X:\\加工待ち"
```

---

## into_folder

```yaml
into_folder: "D:\\CAD_end"
```

### 説明

ファイル移動先フォルダです。

対象ファイルはこのフォルダへ移動されます。

### 注意点

* フォルダが存在しない場合はエラーになります
* ネットワークドライブも使用可能です

### 使用例

```yaml
into_folder: "X:\\共有CAD"
```

```yaml
into_folder: "\\\\SERVER\\CAD"
```

---

## bg_color

```yaml
bg_color: "#00a3af"
```

### 説明

ステータスバーアイコンの背景色です。

### 使用形式

HTMLカラーコード形式。

```yaml
bg_color: "#ff0000"
```

### 色の例

| 色    | コード       |
| ---- | --------- |
| 赤    | `#ff0000` |
| 緑    | `#00aa00` |
| 青    | `#0000ff` |
| オレンジ | `#ff8800` |
| グレー  | `#808080` |

---

## str_color

```yaml
str_color: "white"
```

### 説明

ステータスバー文字色です。

### 使用例

```yaml
str_color: "white"
```

```yaml
str_color: "black"
```

### 推奨

背景色が濃い場合：

```yaml
str_color: "white"
```

背景色が明るい場合：

```yaml
str_color: "black"
```

---

## ignore

```yaml
ignore:
  - "tool"
  - "readme.txt"
  - "template.sx"
```

### 説明

移動対象から除外するファイル・フォルダを指定します。

---

## ignore の動作

| 設定              | 意味          |
| --------------- | ----------- |
| `"tool"`        | toolフォルダを除外 |
| `"readme.txt"`  | 指定ファイルを除外   |
| `"template.sx"` | 指定ファイルを除外   |

---

## フォルダ除外

```yaml
ignore:
  - "tool"
```

### 除外対象例

```text
D:\CAD_start\tool
```

以下すべて除外されます。

```text
tool\
tool\abc\
tool\data\
```

---

## ファイル除外

```yaml
ignore:
  - "readme.txt"
```

### 除外対象

```text
readme.txt
```

という名前のファイル。

---

## 複数指定

```yaml
ignore:
  - "tool"
  - "temp"
  - "readme.txt"
  - "sample.dxf"
```

---

## 空設定

除外なしの場合：

```yaml
ignore: []
```

または：

```yaml
ignore:
```

---

## YAML記述時の注意点

---

## 1. インデントはスペース

YAML はインデントが重要です。

### 正しい例

```yaml
move_of_file_1:
  label: "CAD"
```

### NG例

```yaml
move_of_file_1:
label: "CAD"
```

---

## 2. タブ禁止

YAML は基本的に：

```text
Tabキー禁止
```

です。

必ずスペースを使用してください。

---

## 3. Windowsパスは \ を使用

### 正しい例

```yaml
out_of_folder: "D:\\CAD"
```

### NG例

```yaml
out_of_folder: "D:\CAD"
```

---

## 推奨設定例

---

## CAD用

```yaml
move_of_file_1:
  label: "CAD共有"

  out_of_folder: "D:\\CAD"

  into_folder: "X:\\CAD"

  bg_color: "#00a3af"

  str_color: "white"

  ignore:
    - "tool"
    - "template.sx"
```

---

## レーザー加工用

```yaml
move_of_file_2:
  label: "レーザー"

  out_of_folder: "D:\\Laser"

  into_folder: "X:\\Laser"

  bg_color: "#ff8800"

  str_color: "black"

  ignore:
    - "tool"
    - "sample.dat"
```

---

## 加工機用

```yaml
move_of_file_3:
  label: "加工機"

  out_of_folder: "D:\\NC"

  into_folder: "X:\\NC"

  bg_color: "#ff4444"

  str_color: "white"

  ignore:
    - "tool"
    - "template.nc"
```

---

## ステータスバー操作方法

| 操作      | 動作         |
| ------- | ---------- |
| 左クリック   | ファイル移動実行   |
| 右クリック   | モード切替      |
| ダブルクリック | 移動元フォルダを開く |

---

## 推奨運用

### 推奨

* テンプレートファイルを ignore 登録
* tool フォルダを ignore 登録
* 共有フォルダはネットワークドライブ推奨

---

## 非推奨

* Windowsシステムフォルダ指定
* OneDrive同期競合フォルダ
* アクセス権限不足フォルダ

---

## トラブル時確認項目

| 症状           | 確認内容               |
| ------------ | ------------------ |
| ファイル移動されない   | out_of_folder 存在確認 |
| エラー表示        | into_folder アクセス権  |
| ignore が効かない | スペル確認              |
| 色が変わらない      | カラーコード確認           |
| フォルダ開けない     | パス確認               |
