## ファイル移動設定 詳細マニュアル

この設定は、CAD Support Tool の「ファイル移動機能」の動作を制御します。
YAML ファイル内の `move_of_file_1` ～ `move_of_file_3` を変更することで、移動先や除外ファイルなどを自由に設定できます。

---

## 基本構造

```yaml
## ファイル移動設定
move_of_file_1:
  label: "CADフォルダへ"

  out_of_folder: "D:\\CAD_start"
  into_folder: "D:\\CAD_end"

  bg_color: "#00a3af"
  str_color: "white"

  ignore:
    - "tool"
    - "readme.txt"
    - "template.sx"
```

---

## 各パラメータの詳細

---

## 1. move_of_file_1 (ファイル移動設定の識別名)

```yaml
move_of_file_1:
```

### 説明

現在のアプリでは：

* `move_of_file_1`
* `move_of_file_2`
* `move_of_file_3`

の3種類を切り替えて使用できます。

---

## 2. label (ステータスバーに表示される名称)

```yaml
label: "CADフォルダへ"
```

---

## 3. out_of_folder (ファイル移動元フォルダ)

```yaml
out_of_folder: "D:\\CAD_start"
```

### 説明

このフォルダ内のファイルを監視し、移動対象を検索します。


### 使用例

```yaml
out_of_folder: "C:\\CAD"
```

```yaml
out_of_folder: "X:\\加工待ち"
```

---

## 4. into_folder (ファイル移動先フォルダ)

```yaml
into_folder: "D:\\CAD_end"
```

### 説明

対象ファイルはこのフォルダへ移動されます。

### 注意点

* フォルダが存在しない場合はエラーになります
* ネットワークドライブも使用可能です

### 使用例

```yaml
into_folder: "X:\\共有CAD"
```

---

## 5. bg_color (ステータスバーアイコンの背景色)

```yaml
bg_color: "#00a3af"
```


## 6. str_color (ステータスバー文字色)

```yaml
str_color: "white"
```

### 使用例

```yaml
str_color: "black"
```

---

## 7. ignore (移動対象から除外するファイル・フォルダを指定)

```yaml
ignore:
  - "tool"
  - "readme.txt"
  - "template.sx"
```

## ignore の動作

| 設定              | 意味          |
| --------------- | ----------- |
| `"tool"`        | toolフォルダを除外 |
| `"readme.txt"`  | 指定ファイルを除外   |
| `"template.sx"` | 指定ファイルを除外   |



## 8. YAML記述時の注意点

## Windowsパスは \ \ を使用

### 正しい例

```yaml
out_of_folder: "D:\\CAD"
```

### NG例

```yaml
out_of_folder: "D:\CAD"
```


## 9. ステータスバー操作方法

| 操作      | 動作         |
| ------- | ---------- |
| 左クリック   | ファイル移動実行   |
| 右クリック   | モード切替      |
| ダブルクリック | 移動元フォルダを開く |


## 10. トラブル時確認項目

| 症状           | 確認内容               |
| ------------ | ------------------ |
| ファイル移動されない   | out_of_folder 存在確認 |
| エラー表示        | into_folder アクセス権  |
| ignore が効かない | スペル確認              |
| 色が変わらない      | カラーコード確認           |
| フォルダ開けない     | パス確認               |
